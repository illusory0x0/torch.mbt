{
    "targets": {
        "torch_native.mbt": ["native"],
        "fallback.mbt": ["not", "native"]
    },
    "link": {
        "native": {
            "cc": "g++",
            // "cc": "/home/undef/moonbit/tch-mbt/mycc", # simple script to print args and forward to g++
           "cc-flags": "./torch/native_stub.cpp",
           "cc-link-flags": "-I . -L. -ltchproxy -lm"
        }
    }
}